# Supabase credentials for local pipeline → production DB
# Copy this file to python/.env and fill in your values.
# .env is gitignored — never commit the actual keys.

SUPABASE_URL=https://pqyceostpukueydwuiut.supabase.co
# Get this from: Supabase Dashboard → Settings → API → Service role key (secret)
SUPABASE_SERVICE_ROLE_KEY=
